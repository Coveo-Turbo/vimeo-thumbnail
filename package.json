{
    "name": "@coveops/vimeo-thumbnail",
    "version": "1.0.0",
    "description": "Vimeo Thumbnail with its own modal preview box",
    "main": "dist/index.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "build": "coveops build VimeoThumbnail --template typescript",
        "watch": "coveops build VimeoThumbnail --template typescript --watch",
        "serve": "coveops serve",
        "precompile": "npm run build"
    },
    "keywords": [],
    "author": "",
    "license": "Apache-2.0",
    "types": "dist/index.d.ts",
    "files": [
        "dist"
    ],
    "dependencies": {
        "@coveops/turbo-core": "^1.1.1",
        "coveo-search-ui": "^2.9659.2",
        "exports-loader": "^1.1.0",
        "svg-inline-loader": "^0.8.2"
    },
    "devDependencies": {
        "@coveops/cli": "^0.11.0"
    }
}
